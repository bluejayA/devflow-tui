// Port traits are defined as enum dispatch patterns in the adapter layer.
// This module is reserved for future abstractions if needed.
// Currently, GitProvider/FileWatcher/HooksReceiver use direct implementations
// with mock variants for testing.
